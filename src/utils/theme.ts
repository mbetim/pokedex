import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#E2350D",
    },
    text: {
      primary: "#333333",
    },
  },
  typography: {
    fontFamily: "Titillium Web",
  },
});
